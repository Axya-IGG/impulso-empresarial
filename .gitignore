.DS_Store
Thumbs.db
*.env
.env.local

# Arquivos-fonte de design (originais em alta). O site usa as versões
# otimizadas em assets/ — geradas a partir desta pasta.
impulso empresarial 2 edição/

# Config gerada no diretório temporário de publicação pelo deploy.ps1.
# A versionada é a wrangler.pages.toml.
wrangler.toml

# Vars locais para `wrangler pages dev` (senha/segredo de teste, nunca os reais).
.dev.vars

# Cache/estado do wrangler dev local (build temporário, banco D1 sqlite local).
.wrangler/
